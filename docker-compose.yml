services:
  api:
    build: .
    container_name: banco_moderno_api
    ports:
      - "8000:8000"
    volumes:
      - ./sql_app.db:/app/sql_app.db
    environment:
      - BACKEND_CORS_ORIGINS=["http://localhost:8000"]
    restart: always

networks:
  default:
    name: banco_moderno_network
